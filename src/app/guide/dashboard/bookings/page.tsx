import { Text } from "@mantine/core";

export const metadata = {
  title: "Guide Dashboard - Bookings | Mihape",
};

export default function GuideDashboardBookingsPage() {
  return <Text>Guide Bookings Page (stub)</Text>;
}
