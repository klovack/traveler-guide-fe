import { Text } from "@mantine/core";

export const metadata = {
  title: "Guide Dashboard - Profile | Mihape",
};

export default function GuideDashboardProfilePage() {
  return <Text>Guide Profile Page (stub)</Text>;
}
