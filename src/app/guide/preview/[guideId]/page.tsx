import { Text } from "@mantine/core";

export const metadata = {
  title: "Guide Preview | Mihape",
};

export default function GuidePreviewPage() {
  return <Text>Guide Preview Page (stub)</Text>;
}
